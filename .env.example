# Environment variables for foundry-agent-mcp demo
# Copy this file to .env and fill in required values.
# You can then `source .env` (bash/zsh) or use a dotenv loader.

# REQUIRED: Azure AI Project endpoint (usually your Azure OpenAI or Azure AI hub endpoint)
# Supported regions are westus, westus2, uaenorth, southindia, and switzerlandnorth.
# Format example: https://my-ai-resource.openai.azure.com
PROJECT_ENDPOINT="https://your-project-endpoint"

# REQUIRED: Deployed model deployment name (as configured in Azure)
MODEL_DEPLOYMENT_NAME="your-model-deployment"

# OPTIONAL: MCP server URL and label (defaults set in code)
MCP_SERVER_URL="https://gitmcp.io/Azure/azure-rest-api-specs"
MCP_SERVER_LABEL="github"

# OPTIONAL: Override logging level (unimplemented placeholder for future extension)
# LOG_LEVEL="INFO"

# OPTIONAL: If you prefer explicit credential env vars instead of az login, set:
# AZURE_CLIENT_ID="00000000-0000-0000-0000-000000000000"
# AZURE_TENANT_ID="11111111-1111-1111-1111-111111111111"
# AZURE_CLIENT_SECRET="your-client-secret"

# After editing, load with:
#   export $(grep -v '^#' .env | xargs)
